/**
 * Create a function isDead(health)
 * that returns 'true' if health is equal or lower than zero
 */
function isDead(health) {
  if(health <= 0) {
    return true;
  } else {
    return false;
  }
}
console.log(isDead(-10));

/**
 * Create a function increasePopulation(population, birthRate)
 * that returns new population based on old population and birth rate
 */

/**
 * Create a function decreasePopulation(population, deathRate)
 * that returns new population based on old population and death rate
 */

/**
 * Create a function attack(baseAttack, attackPower)
 * that returns total enemy attack using formula baseAttack*attackPower
 */